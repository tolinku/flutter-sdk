name: tolinku_example
description: Minimal app exercising the Tolinku SDK, used to build the native side in CI.
publish_to: none
version: 1.0.0

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.10.0'

dependencies:
  flutter:
    sdk: flutter
  tolinku:
    path: ../

flutter:
  uses-material-design: true
